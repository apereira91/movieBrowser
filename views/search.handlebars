{{> navbar-info }}


<div class="container">
    <div>
      <h3 >Search results for: {{searchstring}}</h3>
    </div>

  <div class="row d-flex flex-row flex-wrap">
    {{#each results}}
      {{> card }}
    {{/each}}
  </div>

</div>
