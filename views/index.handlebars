{{> navbar }}

<div class="container">
   <div>
      <h3 >{{page_message}}</h3>
    </div> 
  <div class="row d-flex flex-row flex-wrap">
    {{#each results}}
      {{> card }}
    {{/each}}
  </div>

</div>
